<h1 class="list-group"><%= @cocktail.name %></h1>
<% @cocktail.doses.each do |dose| %>
  <%= dose.description %>
  <%= dose.ingredient.name %>
  <!-- <%= dose_path(dose) %> -->
  <%= link_to 'delete', dose_path(dose), method: :delete %>
  <% end %>

<%= link_to 'Add new dose', new_dose_path(@cocktail) %>
<%= link_to 'Back', cocktails_path %>
